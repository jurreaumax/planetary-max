# vite.config.js maps only the resolved API URL into the browser bundle.
API_BASE_URL=https://planetary-max.jurreaumax.workers.dev

# VITE_API_BASE_URL is also supported for conventional Vite deployments.
# VITE_API_BASE_URL=https://api.portal-os.com

# Do not enable the custom domain until its Cloudflare DNS and TLS are active.
# PORTAL_OS_CUSTOM_DOMAIN_ACTIVE=true
